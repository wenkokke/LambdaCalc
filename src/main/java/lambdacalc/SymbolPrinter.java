package lambdacalc;

public interface SymbolPrinter {
	
	/**
	 * Formats a symbol expression.
	 * 
	 * @param symbol the symbol expression
	 * @return a String representation
	 */
	String format(Symbol symbol);
}
