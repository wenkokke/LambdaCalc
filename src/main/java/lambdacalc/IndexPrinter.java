package lambdacalc;

public interface IndexPrinter {
	
	/**
	 * Formats an index expression.
	 * 
	 * @param index the index expression
	 * @return a String representation
	 */
	String format(Index index);
}
